import ReactMarkdown from 'react-markdown'
import reactGfm from 'remark-gfm'

const resumo = "Fala, Etequiano! Aqui é a **noteTec LM**, sua inteligência artificial focada em te fazer gabaritar as provas da ETEC! \n\nAnalisei o material que você enviou e já identifiquei a matéria. Estamos falando de **Banco de Dados**, especificamente sobre os **Tipos de Dados no MySQL / MariaDB**, conteúdo do famigerado **Professor Luís Carlos Pompeu**.\n\n### 📝 Como é a prova do Professor Pompeu?\nO Pompeu tem um estilo bem definido: as provas dele costumam ter **várias questões de múltipla escolha** (focadas em conceitos, limites de dados e sintaxe) e **algumas questões práticas** onde você precisará definir a estrutura de uma tabela ou identificar o tipo de dado correto para uma situação real.\n\nPara te ajudar a focar no que realmente cai, preparei um resumo cirúrgico do PDF. Papel e caneta na mão e vamos lá!\n\n---\n\n# 🧠 Resumo de Elite: Tipos de Dados no MySQL\n\nAntes de criar qualquer tabela, precisamos definir como a informação será armazenada. Isso economiza memória e deixa o banco rápido. \n\n## 1. Dados Numéricos (Os queridinhos das pegadinhas de prova)\n\nAqui moram os pontos onde o Pompeu adora formular questões de múltipla escolha:\n\n*   **Signed vs. Unsigned (Assinado vs. Não Assinado):**\n    *   **SIGNED (Padrão):** Aceita valores negativos e positivos.\n    *   **UNSIGNED:** **Não** aceita valores negativos (começa do 0). Isso dobra o limite positivo do tipo de dado!\n    *   *Exemplo de Prova:* O tipo `TINYINT` tem capacidade para 255 valores. \n        *   Se for **Signed**: vai de `-128` a `127`.\n        *   Se for **Unsigned** (`TINYINT UNSIGNED`): vai de `0` a `255`.\n*   **ZEROFILL:** Preenche os espaços vazios à esquerda com zeros. **Atenção:** se você usar `ZEROFILL`, o campo vira automaticamente `UNSIGNED`.\n    *   *Exemplo:* `TINYINT(4) ZEROFILL` guardando o número 1 vira `0001`.\n*   **Inteiros mais comuns:**\n    *   `TINYINT` (1 byte): Muito pequeno (ótimo para idades).\n    *   `BOOL / BOOLEAN`: Na prática, é um `TINYINT(1)`. `0` é falso, `1` é verdadeiro.\n    *   `INT` ou `INTEGER` (4 bytes): O padrão para chaves primárias e números inteiros comuns.\n    *   `BIGINT` (8 bytes): Números gigantescos.\n*   **Ponto Flutuante vs. Ponto Fixo (Muito importante para a prática!):**\n    *   `FLOAT` (Precisão simples - 7 casas decimais) e `DOUBLE` (Precisão dupla - 15 casas decimais). Use com cuidado, pois podem dar pequenas divergências em cálculos complexos.\n    *   `DECIMAL(M, D)`: **Ponto fixo. Esse cai na prova!** É usado para dinheiro/valores exatos. \n        *   **M** = Total de dígitos (precisão).\n        *   **D** = Quantidade de casas depois da vírgula.\n        *   *Pegadinha de Prova:* Se você declarar `DECIMAL(10,2)`, significa que o número terá **8 dígitos antes** da vírgula e **2 dígitos depois** (totalizando 10 dígitos), e NÃO 10 antes e 2 depois!\n\n---\n\n## 2. Configurações e Tipos de Texto (Strings)\n\nNas questões práticas, você precisará saber qual tipo de texto usar para otimizar o banco.\n\n*   **CHAR[(M)] vs. VARCHAR[(M)] (Cai sempre!):**\n    *   `CHAR`: Tamanho **fixo** (0 a 255). Se você definir `CHAR(10)` e salvar \"Oi\", o MySQL gastará espaço de 10 caracteres de qualquer forma. Bom para siglas (SP, RJ) ou CPF (tamanho fixo).\n    *   `VARCHAR`: Tamanho **variável** (0 a 65535). Se definir `VARCHAR(100)` e salvar \"Oi\", ele só gastará o espaço de 2 caracteres (+1 byte de controle). Excelente para nomes, e-mails e textos dinâmicos.\n*   **TEXT vs. BLOB:**\n    *   `TEXT`: Guarda grandes blocos de texto (ex: artigos).\n    *   `BLOB`: Guarda dados binários (ex: imagens, arquivos PDF).\n*   **Configurações de Texto:**\n    *   `CHARSET` (Character Set): Conjunto de caracteres (ex: `UTF-8` é o mais usado por aceitar acentos e caracteres especiais).\n    *   `COLLATE`: Regras de comparação e ordenação de strings (ex: definir se o banco diferencia maiúsculas de minúsculas ou acentos).\n\n---\n\n## 3. Tipos de Data e Hora\n\nPara campos de cadastro, histórico e logs.\n\n*   `DATE`: Apenas data (`AAAA-MM-DD`).\n*   `TIME`: Apenas hora (`HH:MM:SS`).\n*   `DATETIME`: Data e hora juntas (`AAAA-MM-DD HH:MM:SS`).\n*   `TIMESTAMP`: Semelhante ao DATETIME, mas muito usado para registrar o momento exato em que uma linha foi inserida/alterada (grava o fuso horário).\n\n---\n\n### 🎯 Principais Pontos que serão cobrados pelo Pompeu:\n\nComo a prova dele mescla múltipla escolha com prática, atente-se a:\n\n1.  **Questão Teórica (Múltipla Escolha):** Ele pode te dar um valor (ex: `150`) e perguntar se ele cabe em um `TINYINT` assinado (SIGNED). *Resposta: Não, pois o limite assinado vai até 127. Precisaria ser UNSIGNED.*\n2.  **Questão Prática (Definição de Tabela):** Ele pode pedir para você estruturar os campos de uma tabela cliente. Lembre-se de usar os tipos corretos:\n    *   `id_cliente`: `INT`\n    *   `nome`: `VARCHAR(100)` (tamanho variável)\n    *   `cpf`: `CHAR(11)` (tamanho fixo)\n    *   `salario`: `DECIMAL(8,2)` (dinheiro)\n    *   `data_nascimento`: `DATE`\n3.  **Diferença entre FLOAT e DECIMAL:** Lembre-se de que o `DECIMAL` é exato (ideal para finanças) e o `FLOAT` é aproximado.\n\nEstude essa estrutura, compreenda a lógica de economia de espaço dos tipos de dados e você estará mais do que pronto para encarar a avaliação do Pompeu! Boa sorte e conte comigo! 🚀"
const Res = () => {
    return (<ReactMarkdown remarkPlugins={[reactGfm]}>{resumo}</ReactMarkdown>)
}

export default Res